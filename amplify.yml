version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd vendorpreview
        - npm ci

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: vendorpreview/.next/standalone
    files:
      - '**/*'

  cache:
    paths:
      - vendorpreview/node_modules/**/*
